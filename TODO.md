# TODO

## QA 탐험 봇 (1단계 완료 — `tools/qa-agent/`)

웹소켓/HTTP으로 접속해 이것저것 시도해보고, 발견한 문제를 제안 게시판
(`/api/suggestions`)에 등록하는 도구. 애초에 고려하던 두 방향(스크립트형
휴리스틱 vs LLM 주도형) 대신 세 번째 방향으로 구현: **판단은 이 도구를
부리는 AI 에이전트가 하고, 도구 자체는 브라우저 없이 명령 하나 보내고
응답 모아 받는 얇은 CLI(`qa-client.js`)만 제공.** LLM 주도형의 설명력은
가져가면서, 봇 자체에 LLM 호출을 내장할 필요가 없어 별도 인프라/비용이
안 든다. 사용법은 `tools/qa-agent/README.md` 참고.

세션형이라 에이전트가 붙어서 턴을 굴려줘야 동작 — 무인 상시 실행이
필요해지면 원래 고려하던 스크립트형(휴리스틱) 봇을 별도로 추가하는 걸
고려.

## Phase E — LLM 에이전트 자율 운영 (보류)

마을을 LLM 에이전트가 자율적으로 개척/성장시키고, 다른 유저의 마을을 습격하거나
반대로 습격당하는 시나리오. 설계 시작 전. 지역 군주 시스템(Phase A~D)이 이미
`village`/`raid` 명령 기반이라, 에이전트는 결국 이 명령들을 텍스트로 호출하는
클라이언트 하나를 붙이면 될 것으로 예상 — 별도 API가 필요할지는 착수할 때 결정.

## 지역 군주 시스템 개선 (완료)

Phase A~D 진행하면서 스코프 밖으로 남겨뒀던 미비점 3가지. 전부 구현/검증 완료.

### 1. 마을 출입 제한 ✅

레벨 3(`RAID_UNLOCK_LEVEL`) 미만 마을은 마을원(영주 포함)만 `travel`로 들어갈
수 있음. 레벨 3 이상부터는 누구나 진입 가능.

구현하면서 발견한 문제: 이 제한을 넣으면 `village join`이 "그 마을 안에
있어야 가입 가능" 방식이라 아무도 신생 마을에 가입할 수 없는 데드락이
생김 — `village join <마을이름>`을 원격 가입(어디서든 이름으로 가입)으로
바꿔서 해결.

### 2. 영주 승계 / 마을 해체 ✅

- `village transfer <마을원이름>`: 영주 권한 위임.
- `village disband`: 마을 완전 해체(수비대 제거, DB row 삭제, 월드에서
  방 unregister, 그 자리에 있던 사람 전원 미개척지로 강제 이동).
- `MobManager.despawnMob`, `World.unregisterRoom` 인프라 추가.

### 3. 수비대 해고 ✅

`village garrison remove <몬스터이름>`. 환불 없음.
